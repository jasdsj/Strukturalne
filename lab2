#include<stdio.h>
#include<math.h>

int main()
{
    /*int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    int d =(b*b)-(4*a*c);
    if (d<0)
    {
        printf("Brak rozwiazan");
    }
    else if(d==0)
    {
        printf("Rownanie ma jedno rozwiazanie %f", -b/(2.0*a));
    }
    else
    {
        printf("Rownanie ma dwa rozwiazania %f oraz %f", (-b+sqrt(d))/(2.0*a),(-b-sqrt(d))/(2.0*a));
    }

    printf("Podaj 1-Kwadrat, 2-prostokat, 3-trojkat");
    int w;
    int a,b,h;
    scanf("%d",&w);

    switch(w)
    {
case 1:
    printf("Podaj bok kwadratu");
    scanf("%d",&a);
    printf("Pole kwadratu wynosi %d", a*a);
    break;
case 2:
    printf("Podaj boki prostokata");
    scanf("%d%d",&a,&b);
    printf("Pole prostakata wynosi %d", a*b);
    break;
case 3:
    printf("Podaj bok oraz wysokosc trojkata");
    scanf("%d%d",&a,&h);
    printf("Pole trojkata wynosi %d", a*h/2);
    break;
    }

    int n,m;
    scanf("%d%d",&n,&m);
    int i;
    for(i=1;i<m;i++)
    {
        printf("%d\n",i*n);
    }

    int n,m;
    scanf("%d%d",&n,&m);
    int i;
    for(i=1;i<m/n;i++)
    {
        if(i*n<m)
        printf("%d\n",i*n);
    }
     int n,m;
    scanf("%d%d",&n,&m);
    int i;
    for(i=1;i<=m;i++)
    {
        printf("%d\n",i);
    }

 int n,m,k;
    scanf("%d%d%d",&n,&m,&k);
    int i;
    for(i=m/n;i<k;i+n)
    {

        printf("%d\n%",i);
    }


    int n;
    scanf("%d",&n);
    int i;
    int temp=1;
    for (i=1;i<=n;i++)
    {
        temp *= i;
    }
        printf("%d\n%",temp);


      int n;
    scanf("%d",&n);
    int i;
    int temp=0;
    for (i=1;i<=n;i++)
    {

        temp += i*i;

        printf("%d\n",temp);
    }

    int n;
    scanf("%d",&n);
    int i;
    int temp = 1;
    for (i=2; i<=n; i+2)
    {
    temp *= i;
    printf("%d",temp);
    }

    int n,m;
    scanf("%d%d",&n,&m);
    int min;
    int i;
    int d=1;
    if(n<m){
        min = n;
    }
    else{
        min = m;
    }
    for(i=2;i<=min;i++){
        if(n%i==0&&m%i==0)
        {
            d=i;
        }
    }
    printf("%d",d);*/
